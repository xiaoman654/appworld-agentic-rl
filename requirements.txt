# Install PyTorch separately to match the runtime CUDA.
# Kaggle T4 usually ships with torch already installed.
# AutoDL example:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124

# AppWorld
appworld>=0.1.0

# Hugging Face training stack
transformers>=4.51.0
trl>=0.15.0
accelerate>=1.4.0
peft>=0.14.0
datasets>=3.2.0
evaluate>=0.4.3
bitsandbytes>=0.45.0
safetensors>=0.5.2
tokenizers>=0.21.0
sentencepiece>=0.2.0
protobuf>=5.29.0
einops>=0.8.0

# Data processing
numpy>=1.26.0
pandas>=2.2.0
pyarrow>=18.0.0
scipy>=1.13.0
scikit-learn>=1.5.0
jsonlines>=4.0.0
orjson>=3.10.0
pydantic>=1.9.0,<2.0.0
tenacity>=9.0.0

# Configs and CLIs
hydra-core>=1.3.2
omegaconf>=2.3.0
typer>=0.15.0
rich>=13.9.0
tqdm>=4.67.0
python-dotenv>=1.0.1
packaging>=24.2

# Experiment tracking and visualization
wandb>=0.19.0
tensorboard>=2.18.0
matplotlib>=3.9.0
seaborn>=0.13.2
plotly>=5.24.0
networkx>=3.4.0

# Notebooks and development
ipykernel>=6.29.0
jupyter>=1.1.1
pytest>=8.3.0
ruff>=0.9.0

# Optional on AutoDL A800 for faster rollout / GRPO generation.
# Install manually if needed, because vLLM can be heavy or conflict with Kaggle images:
#   pip install vllm
#
# Optional distributed training:
#   pip install deepspeed
#
# Optional attention kernels, install only if the wheel/build matches your CUDA image:
#   pip install flash-attn --no-build-isolation
